#include <gtest/gtest.h>
#include "Game.h"

TEST(Game, ctor)
{
    // Game::init(nullptr);
    // EXPECT_EQ(Game::player->m_position.m_x, 640.0f);
    // EXPECT_EQ(Game::player->m_position.m_y, 360.0f);
    // EXPECT_EQ(Game::player->m_cur_health, 100);
    // EXPECT_EQ(Game::player->m_max_health, 100);
}
